program ExampleProgram {
    int x = 17;
    real r = 13.1324;
    x = 0;
    read(x, r);
    print(x);
    if (true)
    then print(r);
    fi;
    int rty = x * 2;
    int a = - 5;
    for (int i = 0; i to 12; i by i + 1) {
        if (true && true)
        then print(r);
        fi;
    };
}