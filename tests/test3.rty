program ExampleProgram {
    bool a = 10 < 3;
    for (int i = 0; i to 10; i by i + 1) {
        if (true)
        then print(i);
        fi;
        if (false)
        then read(a);
        fi;
    };
}