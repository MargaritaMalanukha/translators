program ExampleProgram {
    int x = 17;
    real c = 19;
    real r = 13.1324;
    real k = 1908;
    real d = 94828;

    read(x, r);
    print(x, r);

    int rty = x * 2;
    d = 5 / 8;
    int a = -5;

    for (int i = 0; i < 12; i + 1) {
        if (true)
        then print(i);
        fi;
    };
}